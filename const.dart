main(){
    const hour = DateTime.now().hour; //Const variables must be initialized with a constant value or try to replace "const" with "int"
    // int hour = DateTime.now().hour; // The right one
    print(hour);
}